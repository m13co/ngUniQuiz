'use strict';

describe('myApp.2_NewGame module', function() {

  beforeEach(module('myApp.2_NewGame'));

  describe('2_NewGame controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});