'use strict';

describe('myApp.1_Dashboard module', function() {

  beforeEach(module('myApp.1_Dashboard'));

  describe('1_Dashboard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});